[tox]
envlist = py27,py35,py36,py37

[testenv]
deps =
    pytest
commands =
    pytest {posargs}

[testenv:coverage]
deps =
    {[testenv]deps}
    coverage
    coverage-python-version
commands =
    coverage run -m pytest {posargs}
    coverage report -m
